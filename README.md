# 🌱 Raspberry Pi Pico 공기질 측정기

## 1. 프로젝트 이름

**Pico Air Quality Monitor**

Raspberry Pi Pico와 공기 감지 센서를 이용하여 주변 공기 상태를 측정하고, 측정값에 따라 LED로 상태를 표시하는 간단한 공기질 측정기이다.

---

## 2. 무엇을 만들었나

공기 감지 센서에서 들어오는 아날로그 값을 Raspberry Pi Pico가 실시간으로 측정한다.

측정된 값에 따라 공기 상태를 **좋음 / 보통 / 나쁨** 3단계로 구분하고 각각 초록색, 노란색, 빨간색 LED로 표시한다.

* 🟢 초록색 LED : 좋음
* 🟡 노란색 LED : 보통
* 🔴 빨간색 LED : 나쁨

현재 공기질 단계는 실제 공기 오염 농도가 아닌 **센서의 상대적인 측정값을 이용한 실험용 기준**이다.

---

## 3. 어떤 부품으로 만들었나

| 부품                |   수량 | 용도              |
| ----------------- | ---: | --------------- |
| Raspberry Pi Pico |   1개 | 센서값 처리 및 LED 제어 |
| 공기 감지 센서          |   1개 | 주변 공기 상태 감지     |
| 초록색 LED           |   1개 | 공기 상태 '좋음' 표시   |
| 노란색 LED           |   1개 | 공기 상태 '보통' 표시   |
| 빨간색 LED           |   1개 | 공기 상태 '나쁨' 표시   |
| 220~330Ω 저항       |   3개 | LED 전류 제한       |
| 브레드보드             |   1개 | 회로 구성           |
| 점퍼선               | 여러 개 | 부품 연결           |

---

## 4. 어떻게 연결했나

### 공기 감지 센서

| 센서  | Raspberry Pi Pico |
| --- | ----------------- |
| AO  | GP26 (ADC0)       |
| GND | GND               |
| VCC | 센서 사양에 맞는 전원      |

### LED

| LED       | Pico 핀 |
| --------- | ------ |
| 🟢 초록 LED | GP13   |
| 🟡 노랑 LED | GP14   |
| 🔴 빨강 LED | GP15   |

각 LED에는 **220~330Ω 저항**을 연결한다.

### 연결 구조

```text
[공기 감지 센서]
       │
       │ AO
       ▼
     GP26
       │
┌──────▼──────┐
│ Raspberry   │
│ Pi Pico     │
└─────────────┘
   │   │   │
 GP13 GP14 GP15
   │   │   │
   🟢  🟡  🔴
   │   │   │
 저항 저항 저항
   │   │   │
  GND GND GND
```

---

## 5. 어떻게 작동하나

공기 감지 센서의 **AO(Analog Output)**에서 출력되는 전압을 Raspberry Pi Pico의 GP26 ADC 핀에서 읽는다.

Pico의 `read_u16()`을 이용하면 센서값을 **0~65535** 범위의 숫자로 읽을 수 있다.

현재 실험에서 센서값이 약 5,000~9,000 범위에서 변화하는 것을 확인하여 다음과 같이 임시 기준을 설정하였다.

|           센서값 | 상태 | LED   |
| ------------: | -- | ----- |
|     0 ~ 6,499 | 좋음 | 🟢 초록 |
| 6,500 ~ 7,999 | 보통 | 🟡 노랑 |
|      8,000 이상 | 나쁨 | 🔴 빨강 |

센서값은 약 **0.5초마다 한 번씩 측정**한다.

---

## 6. 전체 코드

```python
from machine import ADC, Pin
import time

# 공기질 센서
gas = ADC(Pin(26))

# LED 설정
green = Pin(13, Pin.OUT)
yellow = Pin(14, Pin.OUT)
red = Pin(15, Pin.OUT)

while True:
    # 센서값 읽기
    value = gas.read_u16()

    # 전압으로 변환
    voltage = value / 65535 * 3.3

    # 모든 LED 끄기
    green.off()
    yellow.off()
    red.off()

    # 공기질 판단
    if value < 6500:
        green.on()
        status = "좋음"

    elif value < 8000:
        yellow.on()
        status = "보통"

    else:
        red.on()
        status = "나쁨"

    # 측정 결과 출력
    print(
        value,
        "|", round(voltage, 2), "V",
        "| 공기질:", status
    )

    time.sleep(0.5)
```

---

## 7. 실행 결과

예상 출력:

```text
5537 | 0.28 V | 공기질: 좋음
6977 | 0.35 V | 공기질: 보통
8898 | 0.45 V | 공기질: 나쁨
```

센서값이 변하면 LED도 자동으로 변경된다.

```text
낮은 센서값
     ↓
🟢 좋음

센서값 증가
     ↓
🟡 보통

센서값이 높음
     ↓
🔴 나쁨
```

---

## 8. 프로젝트에서 알게 된 점

이번 프로젝트를 통해 Raspberry Pi Pico의 **ADC(Analog to Digital Converter)**를 이용하여 아날로그 센서의 값을 읽는 방법을 알게 되었다.

또한 센서값에 조건문을 적용하여 LED와 같은 출력 장치를 제어할 수 있다는 것을 확인하였다.

센서에서 출력되는 값은 실제 공기 오염도의 백분율이 아니기 때문에 정확한 공기질 측정기로 발전시키려면 사용하는 센서의 특성을 확인하고 보정하는 과정이 필요하다.

---

## 9. 앞으로 개선할 점

* 센서 모델에 맞는 정확한 공기질 기준 적용
* OLED 디스플레이를 추가하여 측정값 표시
* 부저를 추가하여 공기 상태가 나쁠 때 경고음 발생
* 여러 번 측정한 값의 평균을 이용하여 측정값 안정화
* 시간에 따른 공기질 변화 데이터 저장 및 그래프 표시
